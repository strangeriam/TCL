# 字串格式化

# Syntax:
# format formatstring ?vlue value...?

# Example code 1:
set name John
set age 20

set msg [format "%s is %d years old" $name $age]
john is 20 years old

# 定義單一數字為整數.
set numInt [format %d "88"]

# 定義單一數字為小數(浮點數-實際長度表示).
set numFlot [format %g "8.8"]

# 顏色格式 16進制表示
# output #6358C7
set R 102
set G 88
set B 199
    
set color [format #%02x%02x%02X $r $g $b]
