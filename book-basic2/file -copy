## Copy old log file to folder "old".
set infiles [glob -nocomplain $::PATH_LOG/*.log]
if {[llength $infiles]} {
      file copy {*}$infiles $::PATH_LOG/old
}

## Remove old log file.
if {[llength $infiles]} {
      file delete -force {*}$infiles
}
