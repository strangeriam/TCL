陣列是以字串作為索引. 陣列元素內容的設定使用 set 指令.
Lu-240605: 陣列內容不可使用 變數.

% set price(apple) 10
% set price(orange) 12
% set quantity(apple) 5
% set discount(apple) 0.8
puts $price(apple)
=>10

#===========================
array set familyName {
      papa AgentPAPA
      mama AgentMAMA
      son  AgentSON
}

# or
array set familyName { papa AgentPAPA mama AgentMAMA son AgentSON }

puts $familyName(papa)
# Response: AgentPAPA

puts $familyName(momo)
# Response: AgentMAMA
